# Supabase-Zugang (wird erst in Phase 2 aktiviert).
# Diese Werte sind im Frontend sichtbar (anon key) – das ist by design;
# der Schutz erfolgt über Row Level Security in Supabase.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
